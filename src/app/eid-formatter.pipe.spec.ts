import { EidFormatterPipe } from './eid-formatter.pipe';

describe('EidFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new EidFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
